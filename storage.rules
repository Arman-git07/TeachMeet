rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // This rule allows anyone to read and write to the storage bucket.
    // THIS IS NOT SECURE FOR PRODUCTION.
    // For development purposes only.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
