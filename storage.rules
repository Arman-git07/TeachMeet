rules_version = '2';

// Allow read/write access to all files for any authenticated user
// WARNING: This is for development only. Secure your data before production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
