
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access to the entire database for any user
      // WARNING: This is for development only and is not secure.
      allow read, write: if true;
    }
  }
}
