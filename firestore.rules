
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow read/write to all paths
    // In production, you MUST replace this with secure rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
